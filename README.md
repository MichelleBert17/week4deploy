# Dockerized Node.js API

## Prerequisites

- Node.js (>=16.0.0)
- PostgreSQL database

## Setup

1. Clone this repository.
2. Install dependencies:
   ```bash
   npm install


---

This backend API provides a fully functional starting point. 

It includes:

- A RESTful API with CRUD operations for managing tasks.
- Integration with PostgreSQL using the `pg` library.
- A `Dockerfile` for containerization.


This approach ensures the focus remains on Docker and AWS deployment while minimizing time spent on application development.